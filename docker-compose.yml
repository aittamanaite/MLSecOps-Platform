services:
  dagster:
    build:
      context: .
      dockerfile: src/Dockerfile
    container_name: dagster_orchestrator
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - dagster_home:/app/dagster_home
      - mlflow_data:/app/mlruns
    depends_on:
      - redpanda
    environment:
      - PYTHONPATH=/app
      - DAGSTER_HOME=/app/dagster_home
      - KAFKA_BOOTSTRAP_SERVERS=redpanda:9092
<<<<<<< HEAD
      - STREAM_MAX_RECORDS=500000
=======
      - KAFKA_RAW_TOPIC=raw-logs
      - KAFKA_CLEANED_TOPIC=cleaned-logs
      - KAFKA_ALERT_TOPIC=app-errors
      # --- Volumétrie : 0 = illimité (on exporte TOUT le dataset brut ~2.83M lignes) ---
      - STREAM_MAX_RECORDS=0
>>>>>>> a9b26615667c811ba792659bef9dcc7c1fa578b2
      - PRODUCER_BATCH_SIZE=5000
      - CSV_ENCODING=cp1252
      - KAFKA_MAX_POLL_RECORDS=5000
      - KAFKA_POLL_TIMEOUT_MS=5000
      - KAFKA_IDLE_POLLS_LIMIT=5
      - KAFKA_IDLE_TIMEOUT_S=120
      - EXPORT_APPEND=false
<<<<<<< HEAD
      - MLFLOW_TRACKING_URI=http://mlflow:5000
    command: ["dagster-webserver", "-h", "0.0.0.0", "-p", "3000"]
=======
    command: sh -c 'until python -c "import socket,sys; s=socket.socket(); s.settimeout(1); rc=s.connect_ex((\"redpanda\",9092)); s.close(); sys.exit(0 if rc==0 else 1)" >/dev/null 2>&1; do echo "waiting for redpanda"; sleep 1; done; dagster-webserver -h 0.0.0.0 -p 3000'
>>>>>>> a9b26615667c811ba792659bef9dcc7c1fa578b2

  dagster-daemon:
    build:
      context: .
      dockerfile: src/Dockerfile
    container_name: dagster_daemon
    volumes:
      - .:/app
      - dagster_home:/app/dagster_home
      - mlflow_data:/app/mlruns
    depends_on:
      - dagster
    environment:
      - PYTHONPATH=/app
      - DAGSTER_HOME=/app/dagster_home
      - KAFKA_BOOTSTRAP_SERVERS=redpanda:9092
<<<<<<< HEAD
      - STREAM_MAX_RECORDS=500000
=======
      - KAFKA_RAW_TOPIC=raw-logs
      - KAFKA_CLEANED_TOPIC=cleaned-logs
      - KAFKA_ALERT_TOPIC=app-errors
      # --- Volumétrie : 0 = illimité (on exporte TOUT le dataset brut ~2.83M lignes) ---
      - STREAM_MAX_RECORDS=0
>>>>>>> a9b26615667c811ba792659bef9dcc7c1fa578b2
      - PRODUCER_BATCH_SIZE=5000
      - CSV_ENCODING=cp1252
      - KAFKA_MAX_POLL_RECORDS=5000
      - KAFKA_POLL_TIMEOUT_MS=5000
      - KAFKA_IDLE_POLLS_LIMIT=5
      - KAFKA_IDLE_TIMEOUT_S=120
      - EXPORT_APPEND=false
<<<<<<< HEAD
      - MLFLOW_TRACKING_URI=http://mlflow:5000
    command: ["dagster-daemon", "run"]
=======
    command: sh -c 'until python -c "import socket,sys; s=socket.socket(); s.settimeout(1); rc=s.connect_ex((\"redpanda\",9092)); s.close(); sys.exit(0 if rc==0 else 1)" >/dev/null 2>&1; do sleep 1; done; dagster-daemon run'
>>>>>>> a9b26615667c811ba792659bef9dcc7c1fa578b2

  dbt:
    build:
      context: .
      dockerfile: src/Dockerfile
    container_name: dbt_runner
    working_dir: /app/dbt_project
    depends_on:
      - dagster
    command: ["dbt", "run", "--profiles-dir", "/app/dbt_project"]

  redpanda:
    image: redpandadata/redpanda:v24.1.1
    container_name: redpanda
    command:
      - redpanda
      - start
      - --smp 1
      - --memory 1G
      - --reserve-memory 0M
      - --overprovisioned
      - --node-id 0
      - --kafka-addr PLAINTEXT://0.0.0.0:29092,OUTSIDE://0.0.0.0:9092
      - --advertise-kafka-addr PLAINTEXT://redpanda:29092,OUTSIDE://redpanda:9092
      - --pandaproxy-addr 0.0.0.0:8082
      - --advertise-pandaproxy-addr redpanda:8082
    ports:
      - "8081:8081"
      - "8082:8082"
      - "9092:9092"
      - "29092:29092"
    volumes:
      - redpanda_data:/var/lib/redpanda/data
<<<<<<< HEAD


  mlflow:
    build:
      context: .
      dockerfile: src/Dockerfile
    container_name: mlflow_server
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - mlflow_data:/app/mlruns
    environment:
      - PYTHONPATH=/app
    command: ["mlflow", "server", "--host", "0.0.0.0", "--port", "5000", "--backend-store-uri", "sqlite:///app/mlflow.db", "--default-artifact-root", "/app/mlruns", "--allowed-hosts", "*"]
=======
    healthcheck:
      # rpk est fourni par l'image redpanda (curl n'y est pas présent).
      # On teste le port Kafka directement pour ne pas dépendre de l'admin API.
      test: ["CMD-SHELL", "rpk cluster health -X brokers=127.0.0.1:9092 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s
>>>>>>> a9b26615667c811ba792659bef9dcc7c1fa578b2

volumes:
  redpanda_data:
  dagster_home:
  mlflow_data: